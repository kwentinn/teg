<Window x:Class="PretImmoWPF.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:prism="http://prismlibrary.com/"
		prism:ViewModelLocator.AutoWireViewModel="True"
        Title="{Binding Title, UpdateSourceTrigger=PropertyChanged}" Height="450" Width="800">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="60"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<StackPanel Grid.Row="0" Orientation="Horizontal" DockPanel.Dock="Top" Margin="5" >
			<TextBlock Text="Prêt immo" FontSize="40" />
		</StackPanel>

		<Grid Grid.Row="1">
			
			<Grid.RowDefinitions>
				<RowDefinition Height="*"/>
				<RowDefinition Height="*"/>
			</Grid.RowDefinitions>
			
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="500"/>
				<ColumnDefinition Width="*"/>
			</Grid.ColumnDefinitions>

			<ContentControl Grid.Row="0" prism:RegionManager.RegionName="MainRegion" Margin="5" />
			
			<ContentControl Grid.Row="1" prism:RegionManager.RegionName="ActionRegion" Margin="5" />
			
			<ContentControl Grid.Column="1" Grid.RowSpan="2" prism:RegionManager.RegionName="DetailsRegion" Margin="5" />
		</Grid>
	</Grid>
</Window>
