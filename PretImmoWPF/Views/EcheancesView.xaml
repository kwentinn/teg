<UserControl x:Class="PretImmoWPF.Views.EcheancesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PretImmoWPF.Views"
			 xmlns:prism="http://prismlibrary.com/"
			 prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="50"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>
		<TextBlock Grid.Row="0" Text="Echéancier" FontSize="30" VerticalAlignment="Center" />
		<ListBox x:Name="ListBoxEcheances" Grid.Row="1" 
				 ItemsSource="{Binding CurrentPret.Echeances}">
			<ListBox.ItemTemplate>
				<DataTemplate>
					<StackPanel Orientation="Horizontal">
						<TextBlock Text="Echéance n° " />
						<TextBlock Text="{Binding Id}" />
						<TextBlock Text=" le " />
						<TextBlock Text="{Binding Date}" />
						<TextBlock Text=" - Montant de " />
						<TextBlock Text="{Binding Montant, StringFormat=C2}" />
					</StackPanel>
				</DataTemplate>
			</ListBox.ItemTemplate>
		</ListBox>
	</Grid>
</UserControl>
