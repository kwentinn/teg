<UserControl x:Class="PretImmoWPF.Views.PretView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:PretImmoWPF.Views"
			 xmlns:prism="http://prismlibrary.com/"
			 prism:ViewModelLocator.AutoWireViewModel="True">
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="200" />
			<ColumnDefinition Width="300" />
			<ColumnDefinition Width="*" />
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="40" />
			<RowDefinition Height="40" />
			<RowDefinition Height="40" />
			<RowDefinition Height="40" />
			<RowDefinition Height="40" />
			<RowDefinition Height="40" />
		</Grid.RowDefinitions>
		
		
		<Label Grid.Row="0" Grid.Column="0" VerticalAlignment="Center">Nom du prêt</Label>
		<TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Path=Nom}" VerticalAlignment="Center" />

		<Label Grid.Row="1" Grid.Column="0" VerticalAlignment="Center">Montant du bien</Label>
		<TextBox Grid.Row="1" Grid.Column="1" VerticalAlignment="Center" Text="{Binding Path=MontantBien}" />

		<Label Grid.Row="2" Grid.Column="0" VerticalAlignment="Center">Durée (en années)</Label>
		<TextBox Grid.Row="2" Grid.Column="1" VerticalAlignment="Center" Text="{Binding Path=DureeEnAnnées}" />

		<Button Grid.Row="4" Grid.Column="1" Command="{Binding Path=SaveCommand}" Content="Save" />

	</Grid>
</UserControl>
